"""Docker-based Octave runtime for BasisREMY.

Contains :class:`~basisremy.docker.docker_octave.DockerOctave` and the
``dockerfile`` (plus a ``requirements.txt`` build-context helper) used to build
the Octave image on first use.
"""
